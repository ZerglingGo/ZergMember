language: php
php:
  - hhvm
  - 5.6
  - 7.0
script: phpunit
matrix:
  fast_finish: true
notifications:
  irc:
    channels:
      - "irc.luatic.net:7778#Simpreative"
    template:
      - "%{repository_slug}#%{build_number} (%{branch} - %{commit} : %{author}): %{commit_message}"
      - "Build result : %{result} %{message}"
      - "Build details : %{build_url}"
